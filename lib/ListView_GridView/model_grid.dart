class gridModel {
  final String title, img,year;

  gridModel(this.title, this.img, this.year);

}

// final title = subdata[index]['title'];
// final img = subdata[index]['img'];
// final year = subdata[index]['year'];
